---
title: InterviewEnv
emoji: 🎯
colorFrom: purple
colorTo: green
sdk: docker
pinned: false
---

# InterviewEnv

Train AI to ace job interviews by detecting hidden evaluation rubrics in real time.

## API Endpoints
- `POST /reset` — Start new episode
- `POST /step` — Send candidate answer
- `GET /state` — Get current state
- `GET /tasks` — List all tasks
